# 📦 项目依赖优化建议

## ❌ 可以安全移除的依赖

### 1. `scss` (^0.2.4)

**问题：** 完全未使用

- ❌ 项目中没有 `.scss` 文件
- ❌ 只有一个文件使用 `<style lang="scss">`，但实际是普通CSS
- ✅ 建议移除，节省包体积

**移除命令：**

```bash
pnpm remove scss
```

**影响：** 无任何影响，因为没有使用

### 2. `sass-embedded` (^1.90.0)

**问题：** 开发依赖，但没有 Sass/SCSS 文件

- ❌ 项目中没有实际的 Sass/SCSS 文件
- ✅ 可以移除，节省安装时间

**移除命令：**

```bash
pnpm remove sass-embedded
```

## ⚠️ 可选移除的依赖（看需求）

### 1. 非必要脚本工具

```json
{
  "scripts": {
    "type-check": "vue-tsc --noEmit", // ❌ 没有TypeScript
    "analyze": "npx vite-bundle-analyzer", // ⚠️ 分析工具，可选
    "docker:build": "docker build -t vue-movie .", // ⚠️ Docker相关
    "docker:run": "docker run -p 3000:80 vue-movie" // ⚠️ Docker相关
  }
}
```

### 2. TypeScript 相关（如果不用TS）

- `vue-tsc` - TypeScript检查工具
- `@vue/eslint-config-prettier` - 可能包含TS配置

## ✅ 保留的重要依赖

### 核心功能依赖

1. ✅ `pinyin-pro` - 中文拼音转换（letterFilter.js使用）
2. ✅ `swiper` - 轮播图功能（3个组件使用）
3. ✅ `@iconify/vue` - 图标库（3个页面使用）
4. ✅ `pinia-plugin-persistedstate` - 状态持久化

### 基础依赖

1. ✅ `vue` - 核心框架
2. ✅ `vue-router` - 路由
3. ✅ `pinia` - 状态管理
4. ✅ `axios` - HTTP请求
5. ✅ `element-plus` - UI组件库
6. ✅ `@vueuse/core` - 工具函数库
7. ✅ `vue3-lazy` - 懒加载

## 📊 优化效果预估

### 移除 `scss` + `sass-embedded`

- 📦 减少包体积：约 5-10MB
- ⚡ 减少安装时间：约 10-20秒
- 🧹 清理无用配置

### 移除可选工具

- 📦 减少开发依赖体积
- 🧹 简化 package.json 脚本

## 🛠️ 初学者建议

### 最小化依赖集合（学习用）

```json
{
  "dependencies": {
    "vue": "^3.5.18",
    "vue-router": "^4.5.1",
    "pinia": "^3.0.3",
    "axios": "^1.11.0",
    "element-plus": "^2.10.6"
  }
}
```

### 进阶后再添加

- `@vueuse/core` - 工具函数库
- `swiper` - 轮播图
- `@iconify/vue` - 图标库
- `vue3-lazy` - 懒加载
- `pinyin-pro` - 中文处理

## 🎯 执行建议

### 立即可执行

```bash
# 移除完全未使用的依赖
pnpm remove scss sass-embedded

# 清理 package.json 脚本（可选）
# 手动删除 type-check, docker 相关脚本
```

### 谨慎移除

- 确认不需要 Docker 部署后再移除相关脚本
- 确认不会使用 TypeScript 后再考虑移除 vue-tsc

## 📝 总结

- ❌ **立即移除**：`scss`, `sass-embedded`
- ⚠️ **可选移除**：Docker脚本, TypeScript工具
- ✅ **保留所有**：功能性依赖都在使用
- 🎯 **优化重点**：清理无用样式工具，简化配置
